class Base {
  init(a, b) {
    print("Base.init(" + a + ", " + b + ")");
  }
}

class Derived < Base {
  init() {
    print("Derived.init()");
    super.init("a", "b");
  }
}

Derived();
#=> Derived.init()
#=> Base.init(a, b)
