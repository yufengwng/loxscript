class Base {
  foo(a, b) {
    print("Base.foo(" + a + ", " + b + ")");
  }
}

class Derived < Base {
  foo() {
    super.foo(1); #@! [line 9] runtime error: expected 2 arguments but got 1
  }
}

Derived().foo();
